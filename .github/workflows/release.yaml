# This is a basic workflow to help you get started with Actions
# this name should be the same with file name
name: release

# Controls when the workflow will run
on:
  workflow_call:
  workflow_dispatch:
  push:
    tags:
      - v*

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  compile:
    # The type of runner that the job will run on
    runs-on: ubuntu-24.04
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_GAO_CI }}
      # check out chain-ops to get archive_pkg action
      - uses: actions/checkout@v4
        name: Checkout Chain Ops
        with:
          repository: megaeth-labs/chain-ops
          token: ${{ secrets.PAT_GAO_CI }}
          path: .chain-ops
          sparse-checkout: .github/actions
      - name: Configure Git Credentials
        uses: ./.chain-ops/.github/actions/git_config
        with:
          github_pat: ${{ secrets.PAT_GAO_CI }}

      - name: Install Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          cache: false

      - name: Build
        id: build_step
        run: |
          cargo build --profile release
      - name: Generate Package Info
        id: package_info
        run: |
          COMMIT_ID=$(git rev-parse HEAD)
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION="${GITHUB_REF#refs/tags/}"
          else
            VERSION="latest"
          fi
          ARTIFACT_NAME="stateless-validator-${VERSION}-${COMMIT_ID}"
          BINARY_PATH="$GITHUB_WORKSPACE/target/release/stateless-validator"
          echo "Binary path : $BINARY_PATH"
          echo "artifact_name=${ARTIFACT_NAME}" >> $GITHUB_OUTPUT
          echo "binary_path=${BINARY_PATH}" >> $GITHUB_OUTPUT
      # upload when tagging or trigger in web ui
      - name: Upload Package
        if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/')
        uses: ./.chain-ops/.github/actions/archive_pkg
        with:
          url: ${{ secrets.NEXUS_URL }}
          username: ${{ secrets.NEXUS_USER }}
          password: ${{ secrets.NEXUS_PWD }}
          repository: stateless-validator/release
          artifact_name: ${{ steps.package_info.outputs.artifact_name }}
          file_path: ${{ steps.package_info.outputs.binary_path }}
